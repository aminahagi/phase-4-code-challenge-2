<%= form_with(model: heropower) do |form| %>
  <% if heropower.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(heropower.errors.count, "error") %> prohibited this heropower from being saved:</h2>

      <ul>
        <% heropower.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :strength, style: "display: block" %>
    <%= form.text_field :strength %>
  </div>

  <div>
    <%= form.label :hero_id, style: "display: block" %>
    <%= form.text_field :hero_id %>
  </div>

  <div>
    <%= form.label :power_id, style: "display: block" %>
    <%= form.text_field :power_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
